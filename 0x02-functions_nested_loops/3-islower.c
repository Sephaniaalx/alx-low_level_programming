#include "main.h"
#include <stdio.h>
#include <unistd.h>

/**
 * _islower - function prints the lower character
 *
 * Return 1 if c but 0 if not
 *
 */

int _islower(int c);
{
int i;

for (i = 'a'; i <= 'z'; i++)
{
_putchar(i);
if (i = 'c')
{
return 1;
}
else
{
return 0;
}
}
