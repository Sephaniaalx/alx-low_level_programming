#include <stdlib.h>
#include <stdio.h>

/**
 * main - thats the main function
 * @argc: this represent the count
 * @argv[]: this represents the array
 * @argv: this represents the string
 * Return: returns 0 on success
 */

int main(int argc __attribute((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
