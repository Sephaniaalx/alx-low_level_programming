#include "main.h"
#include <stdio.h>
#include <unistd.h>

/**
 * mul - takes the argument
 *
 * @a: first number
 * @b: second number
 *
 * Return: (0) on success
 */
int mul(int a, int b)
{
return (a * b);
}
