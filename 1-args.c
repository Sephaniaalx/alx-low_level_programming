#include <stdlib.h>
#include <stdio.h>

/**
 * main - the main function
 * @argc: the count
 * @argv: the value
 * Return: 0 upon success
 */

int main(int argc, char* argv[] __attribute((unused)))
{
	printf("%d\n", argc);
	return (0);
}
